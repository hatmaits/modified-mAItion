x-roat-service: &roat-service
  build:
    context: .
    dockerfile: Dockerfile.roat

services:

  # OWUI front-end
  openwebui:
    image: ghcr.io/open-webui/open-webui:0.6.5
    ports:
      - "${WEBUI_BIND_HOST:-127.0.0.1}:${HTTP_WEB_PORT:-3000}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      api:
        condition: service_healthy
      ollama-init:
        condition: service_completed_successfully
    volumes:
      - open-webui:/app/backend/data
      - open-webui-model-cache:/app/backend/data/cache/embedding/models
      - open-webui-cache:/root/.cache
      - ./tools/roat_retrieval.py:/etc/roat_retrieval.py
      - ./tools/roat_retrieval.json:/etc/roat_retrieval.json
      - ./tools/mediawiki_tool.py:/etc/mediawiki_tool.py
      - ./tools/mediawiki_tool.json:/etc/mediawiki_tool.json
      - ./tools/web_search.py:/etc/web_search.py
      - ./tools/web_search.json:/etc/web_search.json
      - ./functions/video_inject.py:/etc/video_inject.py
      - ./functions/video_inject.json:/etc/video_inject.json
      - ./models:/etc/owui-models:ro
      - ./helpers/entrypoint.sh:/custom-entrypoint.sh
      - ./helpers/healthz.py:/etc/healthz.py
      - ./patches/branding.patch:/etc/patches/branding.patch:ro
      - ./static:/app/build/static:ro
      - ./static/favicon.png:/app/build/favicon.png:ro
    entrypoint: /custom-entrypoint.sh
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    # override the same variables from env_file
    environment:
      # see https://docs.openwebui.com/tutorials/integrations/redis#multi-worker-and-multi-instance-deployments
      - REDIS_URL=redis://redis:6379/1
      - ENABLE_WEBSOCKET_SUPPORT=true
      - WEBSOCKET_MANAGER=redis
      - WEBSOCKET_REDIS_URL=redis://redis:6379/2
      - 'WEBSOCKET_REDIS_OPTIONS={"socket_connect_timeout": 5, "socket_timeout": null}'
      - REDIS_KEY_PREFIX="open-webui"
      - DATABASE_URL=postgresql://$POSTGRES_USER:$POSTGRES_PASSWORD@postgres:5432/$POSTGRES_DB
      - DATABASE_ENABLE_SESSION_SHARING=True
      - HEALTHZ_PORT=8081
      - HEALTHZ_READY_FILE=/tmp/healthz_ready
      - ENABLE_EVALUATION_ARENA_MODELS=False
      - ENABLE_COMMUNITY_SHARING=False
      - USER_PERMISSIONS_CHAT_SYSTEM_PROMPT=False
      - USER_PERMISSIONS_FEATURES_DIRECT_TOOL_SERVERS=False
      - USER_PERMISSIONS_FEATURES_CODE_INTERPRETER=False
      - USER_PERMISSIONS_CHAT_CONTROLS=False
      - USER_PERMISSIONS_CHAT_VALVES=False
      - ENABLE_DIRECT_CONNECTIONS=False
      - ENABLE_PIP_INSTALL_FRONTMATTER_REQUIREMENTS=False
      - OLLAMA_DEFAULT_MODEL=${OLLAMA_DEFAULT_MODEL:-qwen3:4b}
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:${HEALTHZ_PORT:-8081}/healthz || exit 1" ]
      interval: 5s
      timeout: 2s
      retries: 5
      start_period: 5s

  # local Ollama inference server
  ollama:
    image: ollama/ollama:0.32.5
    environment:
      - OLLAMA_CONTEXT_LENGTH=${OLLAMA_CONTEXT_LENGTH:-8192}
      - OLLAMA_NOHISTORY=true
    volumes:
      - ollama_data:/root/.ollama
    healthcheck:
      test: [ "CMD", "ollama", "list" ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    restart: unless-stopped

  # pull the configured model before the API and UI start
  ollama-init:
    image: ollama/ollama:0.32.5
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      - OLLAMA_HOST=http://ollama:11434
    entrypoint: [ "/bin/ollama" ]
    command: [ "pull", "${OLLAMA_DEFAULT_MODEL:-qwen3:4b}" ]
    restart: "no"

  # database
  postgres:
    image: ankane/pgvector:v0.5.1
    env_file:
      - .env.rag
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./helpers/01-openwebui-init.sql:/docker-entrypoint-initdb.d/01-openwebui-init.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB} || exit 1" ]
      interval: 5s
      retries: 10
      timeout: 5s
      start_period: 5s

  # redis
  redis:
    image: redis:7
    env_file:
      - .env.rag
    volumes:
      - redis_data:/data
    command: [ "redis-server", "--appendonly", "yes" ]
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      retries: 5
      timeout: 5s
      start_period: 5s

  # rag-of-all-trades API
  api:
    <<: *roat-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      ollama-init:
        condition: service_completed_successfully
    env_file:
      - .env.rag
    environment:
      - OLLAMA_DEFAULT_MODEL=${OLLAMA_DEFAULT_MODEL:-qwen3:4b}
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - api-modesl-cache:/root/.cache
    command: >-
      sh -c "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000"
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:8000/health || exit 1" ]
      interval: 5s
      timeout: 2s
      retries: 5
      start_period: 10s

  # rag-of-all-trades WORKER
  celery_worker:
    <<: *roat-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env.rag
    environment:
      - PYTHONPATH=/app
      - OLLAMA_DEFAULT_MODEL=${OLLAMA_DEFAULT_MODEL:-qwen3:4b}
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ${DIRECTORY_HOST_PATH:-./data/input}:/data/input:ro
    command: >-
      bash -c "celery -A celery_app worker
      --pool=prefork --concurrency=${CELERY_CONCURRENCY:-2}
      --max-tasks-per-child=${MAX_TASK_CHILD:-50}
      --max-memory-per-child=${MAX_MEMORY_PER_CHILD:-300000}
      --loglevel=info
      -Q celery
      -O fair"
    restart: unless-stopped

  # rag-of-all-trades BEAT
  celery_beat:
    <<: *roat-service
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env.rag
    environment:
      - PYTHONPATH=/app
      - OLLAMA_DEFAULT_MODEL=${OLLAMA_DEFAULT_MODEL:-qwen3:4b}
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    command: >-
      bash -c "celery -A celery_app beat --loglevel=info"
    restart: unless-stopped

volumes:
  open-webui:
  open-webui-cache:
  open-webui-model-cache:
  postgres_data:
  redis_data:
  api-modesl-cache:
  ollama_data:
